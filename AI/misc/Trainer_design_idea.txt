for each epoch
    for each phase, DataLoader
        # Train loop
            # Stuff to do:
                + forward_pass
                + log loss, metrics, lr

        # Eval loop
            # Stuff to do
                + forward_pass
                + log loss, metrics
                + save checkpoint based on cond
                + check early stopping cond

Run_batch:
    -> Dispatch to selected batch_running_algorithm

Each batch_running_algorithm will do:
    perform forward_pass on batch manner
    compute loss, metrics
