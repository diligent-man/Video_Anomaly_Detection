from typing import Dict, Any

__all__ = ["prompt_dataset_statistics", "draw_bar_chart"]


def prompt_dataset_statistics(stats: Dict[str, Any]) -> str:
    prompt = ""
    return prompt


def draw_bar_chart(x, y, tiltle, legendsgs) -> None:
    # Co the dung seaborn + matplotlib
    return None

